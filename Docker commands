Docker commands

https://www.youtube.com/watch?v=k6tDraepW24&t=49s

docker
docker info
docker ps
docker start 
docker stop
docker run -d -p 81:80 docker/getting-started
docker pull 
docker docker run -it --name MyUbuntu ubuntu
docker build 
docker image rm id or name
docker run -p 3001:8001 id or name(1326c371eca)

*******
docker container stats
docker compose up --build -d
docker stop $(docker ps -a -q)
docker logs -f {container_id}

где {container_id} - это значение из колонки CONTAINER ID при вызове команды docker ps

